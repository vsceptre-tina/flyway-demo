table "dev2_db"."users" {
  columns = (
    column "id" {
      type = int
      null = NOT NULL
    }
    column "username" {
      type = varchar(50)
      null = NOT NULL
    }
    column "created_at" {
      type = timestamp
      default = CURRENT_TIMESTAMP
    }
    column "phone" {
      type = varchar(20)
    }
    column "email" {
      type = varchar(100)
    }
    column "status" {
      type = varchar(20)
      default = 'active'
    }
  )
  primaryKey = {
    columns = [
      id
    ]
    name = "PRIMARY"
    objectType = primaryKey
  }
}

index "dev2_db"."users"."username" {
  columns = <
    {
      name = username
    }

  >
  type = BTREE
  unique = UNIQUE
}

